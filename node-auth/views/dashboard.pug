extends layout

block content
    if (message)
        .alert.alert-danger.col-sm-12.col-md-6.col-md-offset-3.col-lg-6.col-lg-offset-3.text-center(role='alert') #{message}
    h1 #{title}
    p
    | Hello, Shakib!
    br
    | Welcome to your dashboard.
